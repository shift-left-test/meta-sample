BBPATH =. "${LAYERDIR}:"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-sample"
BBFILE_PATTERN_meta-sample = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-sample = "82"

LAYERDEPENDS_meta-sample = "core meta-testing"
LAYERSERIES_COMPAT_meta-sample = "morty pyro rocko sumo thud warrior zeus"